# Polish checklist (đã áp dụng)

## Tokens & utilities
- `--color-primary` / `--color-primary-soft` — thay violet rải rác
- `.app-surface-elevated` — card/bảng/section
- `.app-modal` — dialog, bulk bar
- `.app-tab-segmented` + `.app-tab-segmented-btn` — tab mobile
- `.app-bottom-nav` — shell mobile

## Copy
- Bỏ đoạn «Cách đọc…», banner mô tả pipeline, hướng dẫn dài ở header
- `KpiCallHint compact` trên mọi view KPI
- `ScoringViewModeHint compact` trên Hồ sơ

## Chưa đổi (cố ý)
- Panel workspace tối (AI / LLM dark) — giữ tone riêng
- Gradient AI modal — chuyển sang primary→accent, không dùng violet
- Hướng dẫn nội dung body — vẫn đầy đủ trong từng section
