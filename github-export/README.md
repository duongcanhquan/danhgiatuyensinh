# GitHub export — danhgiatuyensinh

Snapshot metadata kéo từ GitHub API + `git fetch --all` (không phải clone lại source code — code đã nằm trong repo này).

Xem `inventory.json` để biết tổng quan và danh sách file.
